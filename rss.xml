<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:content="http://purl.org/rss/1.0/modules/content/">
    <channel>
        <title>&lt;cpro95 /&gt;</title>
        <link>/</link>
        <description>Blog of cpro95@gmail.com</description>
        <lastBuildDate>Fri, 18 Jan 2019 06:12:40 GMT</lastBuildDate>
        <docs>http://blogs.law.harvard.edu/tech/rss</docs>
        <generator>https://github.com/jpmonette/feed</generator>
        <item>
            <title><![CDATA[이 블로그는 어떻게 수정하죠? by James K Nelson]]></title>
            <link>\posts\2019-01-05\welcome\</link>
            <guid>\posts\2019-01-05\welcome\</guid>
            <content:encoded><![CDATA[<div><div><div class="document_logo__1gKDE"><img src="/static/media/navi-logo.0f2f9d7f.svg" class="document_logo-navi__1qVdn" alt="logo"/><img src="/static/media/react-logo.ee7cd8ed.svg" class="document_logo-react__2CW3W" alt="logo"/></div><h2 id="how-to">How to…</h2><p><strong>To update this post,</strong> edit <code>src/pages/posts/2019-01-05-welcome/document.mdx</code> and save to reload.</p><p><strong>To update the site’s title,</strong> edit the metadata in <code>src/siteMetadata.js</code>.</p><p><strong>To change the site’s colors,</strong> edit the <code>:root</code> block in <code>src/index.module.css</code>.</p><p><strong>To create a new post,</strong> add a new directory to <code>src/pages/posts</code> that is prefixed with the new post’s publication date, then create a <code>post.js</code> file inside it:</p><pre><code class="language-jsx" data-language="jsx" data-highlighted-line-numbers=""><span class="token keyword">export</span> <span class="token keyword">default</span> <span class="token punctuation">{</span>
  title<span class="token punctuation">:</span> <span class="token template-string"><span class="token string">`Learn How To Build This Blog From Scratch`</span></span><span class="token punctuation">,</span>
  tags<span class="token punctuation">:</span> <span class="token punctuation">[</span><span class="token string">'react'</span><span class="token punctuation">,</span> <span class="token string">'navi'</span><span class="token punctuation">]</span><span class="token punctuation">,</span>
  spoiler<span class="token punctuation">:</span> <span class="token string">"An online course with loads of live demos and exercises."</span><span class="token punctuation">,</span>
  getContent<span class="token punctuation">:</span> <span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">=></span> <span class="token keyword">import</span><span class="token punctuation">(</span><span class="token string">'./document.mdx'</span><span class="token punctuation">)</span><span class="token punctuation">,</span>
<span class="token punctuation">}</span>
</code></pre><p><strong>To set the maximum number of posts on an index page,</strong>, set the <code>indexPageSize</code> property of the object exported by <code>src/siteMetadata.js</code>.</p><p><strong>To modify the generated HTML,</strong> update the <code>src/renderPageToString.js</code> file.</p><p><strong>To add non-blog pages,</strong> just use <a href="https://frontarm.com/navi/reference/declarations/#createpage"><code>Navi.createPage()</code></a>, <a href="https://frontarm.com/navi/reference/declarations/#createswitch"><code>Navi.createSwitch()</code></a> and <a href="https://frontarm.com/navi/reference/declarations/#createredirect"><code>Navi.createRedirect()</code></a>. For an example, check the about page at <code>src/pages/about/index.js</code>. If you create a page, remember to add it to the root switch at <code>src/pages/index.js</code>.</p><p><strong>To brush up on React’s fundamentals,</strong> check out the <a href="https://frontarm.com/courses/learn-raw-react/">React (without the buzzwords)</a> and <a href="https://frontarm.com/courses/async-javascript/">Asynchronous JavaScript</a> courses.</p><h2 id="the-lowdown">The lowdown</h2><p>This template is designed with a JAMstack architecture. This means that the blog is statically rendered for the fastest possible load time, but once loaded, it turns into a single page app — ensuring navigation is silky smooth.</p><p>It follows a convention-over-configuation philosophy. It should work out of the box with minimal effort for most people’s needs, but can also be configured where required.</p><p>Finally, an effort has been made to prefer explicit code over concise code, and simplicity over complexity. Magic is avoided wherever possible, even if this means more code is required for the desired result.</p><h2 id="the-tools">The tools</h2><ul><li><a href="https://reactjs.org">React</a> renders the UI</li><li><a href="https://mdxjs.com">MDX</a> converts markdown into React components</li><li><a href="https://frontarm.com/navi/">Navi</a> handles routing and creates static HTML files for each route</li><li><a href="https://reactjs.org/docs/create-a-new-react-app.html">create-react-app</a> builds the app’s assets and provides the dev server</li></ul></div></div>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[FFMpeg 사용법]]></title>
            <link>\posts\2019-01-10\ffmpeg-tutorial\</link>
            <guid>\posts\2019-01-10\ffmpeg-tutorial\</guid>
            <content:encoded><![CDATA[<div><div><h1 id="동영상-자막-입히기">동영상 자막 입히기</h1><ol><li>ASS 자막으로 변환하기</li></ol><pre><code>ffmpeg -i movie.srt movie.ass
//ass 자막은 Fontname, Fontsize 변경 가능하다
</code></pre><ol start="2"><li>자막 입히기</li></ol><pre><code>ffmpeg -i movie.mkv -vf subtitles=subtitle.ass out.mkv

ffmpeg -i movie.mkv -vf &quot;ass=sustitle.ass&quot; out.mkv

ffmpeg -i movie.mkv -vf subtitles=movie.mkv out.mkv
// 동영상이 자막을 포함하는 경우

ffmpeg -i movie.mkv -vf subtitles=movie.mkv:si=1 out.mkv
// 이 명령어는 mkv 동영상에서 두번째 자막을 선택하라는 뜻
</code></pre><ol start="3"><li>자막 추출</li></ol><pre><code>ffmpeg -i movie.mkv out.srt
// 간단히 out.srt 라고 지정해도 자막이 추출된다.

ffmpeg -i movie.mkv -map 0:15 out.srt
// 자막이 여러개 있을 경우 map 명령어로 STREAM을 지정할 수 있다.
</code></pre><h1 id="동영상-변환">동영상 변환</h1><ol><li>동영상 코덱 관리</li></ol><pre><code>ffmpeg -i movie.mkv -vcodec h264 -acodec aac out.mkv
// 비디오 코덱은 h264, 오디오 코덱은 aac 를 지정한 경우

ffmpeg -i sample.mkv -c:v h264 -c:a aac out.mkv
// 비디오 코덱은 h265, 오디오 코덱은 aac 를 지정한 경우

ffmpeg -i sample.mkv -c:v copy -c:a aac out.mkv
// 비디오 코덱은 copy(기존꺼 그대로), 오디오 코덱은 aac 를 지정한 경우
// 인코딩 속도가 빨라짐.
</code></pre></div></div>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Git 공부하기]]></title>
            <link>\posts\2019-01-11\git-tutorial\</link>
            <guid>\posts\2019-01-11\git-tutorial\</guid>
            <content:encoded><![CDATA[<div><div><h1 id="git-tutorial">Git Tutorial</h1><h2 id="dvcs분산-버전-관리-시스템">DVCS(분산 버전 관리 시스템)</h2><pre><code>git = 분산 버전 관리 시스템(Distributed Version Control System)
</code></pre><p>클라이언트가 저장소 통채로 복제
클라이언트가 마지막 스냅샷을 체크아웃하는 것이 아니다.</p><hr/><h2 id="파일의-상태">파일의 상태</h2><h4 id="tracked-파일의-세-가지-상태">TRACKED 파일의 세 가지 상태</h4><p><img src="https://wikidocs.net/images/page/11069/areas.png" alt="git_status"/></p><pre><code>modified: 파일을 수정했지만 아직 커밋하지 않은 상태
staged: 파일을 수정해서 곧 커밋할 것이라고 표시한 상태
committed: 데이터가 로컬 데이터베이스에 안전하게 저장된 상태
</code></pre><hr/><h2 id="파일의-라이프사이클">파일의 라이프사이클</h2><p><img src="https://wikidocs.net/images/page/11069/lifecycle.png" alt="stage_img"/></p><h5 id="작업-디렉토리working-directory의-파일">작업 디렉토리(Working Directory)의 파일</h5><pre><code>Untracked = 버전 관리 대상이 아닌 파일
Tracked = 버전 관리 대상 파일
      Unmodified = 수정 안 한 파일
      Modified = 수정한 파일
      Staged = 커밋 직전의 파일 = Changes to be committed
</code></pre><p>Untracked, Modified 파일을 추가(add)한다는 것은 다음에 커밋할(Staged) 파일 목록에 추가한다고 생각해야 한다.
프로젝트에 파일을 추가한다는 뜻이 아니다.
그리고 Untracked 파일은 Staged 파일이 되기 때문에 자연스럽게 Tracked 파일이 된다.</p><hr/><h2 id="설정">설정</h2><h4 id="설정-파일">설정 파일</h4><pre><code>/etc/gitconfig</code>  파일: 전역 설정 </pre><pre><code>~/.gitconfig</code> 또는 <code>~/.config/git/config</code> 파일: 사용자 설정</pre><pre><code>.git/config</code> 파일: 저장소 설정</pre><p>설정의 우선순위는 역순이다.</p><h4 id="사용자-정보">사용자 정보</h4><pre><code>$ git config --global user.name &quot;John Doe&quot; $ git config --global user.email &quot;johondoe@example.com&quot;</code></pre><code>git config --global</code> 옵션으로 명령했기 때문에 우분투의 경우 사용자 설정 파일 <code>~/.gitconfig</code> 에 다음 내용으로 저장된다.<pre><code>cat .gitconfig [user] name = John Doe email = johondoe@example.com</code></pre><hr/><h2 id="저장소-만들기">저장소 만들기</h2><h4 id="로컬-현재-디렉토리를-git-저장소로-만들기">로컬 현재 디렉토리를 GIT 저장소로 만들기</h4><pre><code>git init
</code></pre><h4 id="원격-저장소를-로컬로-복제clone하기">원격 저장소를 로컬로 복제(CLONE)하기</h4><pre><code>git clone https://github.com/libgit2/libgit2
// 원격 저장소를 원격 저장소의 이름으로 복제한다.
</code></pre><pre><code>git clone https://github.com/libgit2/libgit2 mylibgit
// 원격 저장소를 mylibgit 새로운 이름으로 복제한다.
</code></pre><h6 id="참고-사항">참고 사항</h6><p>원격 저장소의 이름은 기본값이 origin이다.
권한이 없는 원격 저장소를 clone할 수 있고 오픈소스를 fork한 원격 저장소를 clone할 수도 있다.</p><hr/><h3 id="여러-가지-예시">여러 가지 예시</h3><h6 id="새-파일-저장소에-저장하기-untracked---staged---committed">새 파일 저장소에 저장하기 (UNTRACKED -&gt; STAGED -&gt; COMMITTED)</h6><p>*.c와 LICENSE 새로운 파일을 만들어서 저장소에 저장하는 것은 2단계로 나눌 수 있다.</p><p>1단계 (Untracked -&gt; Staged)</p><pre><code>$ git add *.c
$ git add LICENSE
</code></pre><p>2단계 (Staged -&gt; Committed)</p><pre><code>$ git commit -m &#x27;initial project version&#x27;
</code></pre><hr/><h6 id="파일을-수정하고-저장소에-저장하기-unmodified---modified---staged---committed">파일을 수정하고 저장소에 저장하기 (UNMODIFIED -&gt; MODIFIED -&gt; STAGED -&gt; COMMITTED)</h6><p>처음 저장소를 Clone하면 모든 파일은 Tracked이면서 Unmodified 상태이다.</p><p>1단계: 파일 수정 (Unmodified -&gt; Modified)</p><pre><code>에디터로 hello.c 파일을 편집한다.
</code></pre><p>2단계: 파일 추가 (Modified -&gt; Staged)</p><pre><code>$ git add hello.c
</code></pre><p>3단계: 파일 커밋 (Staged -&gt; Committed)</p><pre><code>$ git commit -m &#x27;greeting message changed&#x27;
</code></pre><p>git add 명령어는 Untracked 파일을 Staged 파일로 만들 때도 쓰고 Modified 파일을 Staged 파일로 만들 때도 사용한다.</p><p>git add의 의미는 프로젝트에 파일을 추가한다기보다 다음에 커밋할 파일의 목록에 추가하는 것으로 이해한다.</p><hr/><h6 id="작업-디렉토리의-파일의-상태-확인">작업 디렉토리의 파일의 상태 확인</h6><pre><code>$ git status
</code></pre><h6 id="파일-무시하기">파일 무시하기</h6><pre><code>.gitignore
// 위 파일을 만들어 버전 관리하지 않고 무시할 파일 목록을 만든다.
</code></pre><h6 id="파일-비교">파일 비교</h6><pre><code>$ git diff
// Modified 파일을 Committed 파일과 비교하여 보여준다.
</code></pre><pre><code>$ git diff --staged
// Staged 파일을 Committed 파일과 비교하여 보여준다.
</code></pre><hr/><h6 id="변경사항-커밋하기">변경사항 커밋하기</h6><pre><code>$ git commit
// 커밋 메시지를 에디터로 입력 후 커밋한다.
</code></pre><pre><code>$ git commit -m &quot;Story 182: Fix benchmarks for speed&quot;
// 커밋 메시지를 옵션으로 지정하고 커밋한다.
</code></pre><pre><code>$ git commit -am &quot;added new benchmarks&quot;
// Staging Area 생략 후 바로 커밋할 수도 있다.
</code></pre><hr/><h6 id="파일을-삭제하기">파일을 삭제하기</h6><pre><code>$ git rm 파일이름
</code></pre><h6 id="파일-이름-바꾸기">파일 이름 바꾸기</h6><pre><code>$ git mv 옛파일이름 새로운파일이름
</code></pre><hr/><h3 id="되돌리기">되돌리기</h3><h6 id="마지막-커밋-내용을-되돌리기">마지막 커밋 내용을 되돌리기</h6><p>이미 커밋을 했지만 파일을 깜빡하고 추가하지 않은 파일이 있거나 커밋 메시지를 수정하고 싶을 때 git commit —amend 명령어를 이용한다.</p><pre><code>$ git commit -m &#x27;initial commit&#x27;
$ git add forgotten_file
$ git commit --amend
</code></pre><hr/><h6 id="staged---unstaged">STAGED -&gt; UNSTAGED</h6><p>git reset HEAD [파일 이름] 명령어로 특정 파일을 Staged 상태에서 Unstaged 상태로 변경한다.</p><pre><code>$ git reset HEAD CONTRIBUTION.md
</code></pre><hr/><h6 id="modified---unmodified">MODIFIED -&gt; UNMODIFIED</h6><p>git checkout — [파일 이름] 명령으로 애초에 저장소에 저장된 버전을 다시 꺼내온다.</p><pre><code>$ git checkout -- CONTRIBUTION.md
</code></pre><hr/><h2 id="원격-저장소remote-repository">원격 저장소(REMOTE REPOSITORY)</h2><h4 id="원격-저장소--인터넷이나-네트워크-어딘가에-존재하는-저장소">원격 저장소 = 인터넷이나 네트워크 어딘가에 존재하는 저장소</h4><h4 id="원격-저장소-확인">원격 저장소 확인</h4><p>원격 저장소를 clone하면 origin 이름으로 원격 저장소가 자동으로 등록된다.</p><pre><code>$ git remote
// 원격 저장소의 이름을 확인한다.
</code></pre><pre><code>$ git remote -v
// 원격 저장소의 이름과 URL을 함께 확인한다.
</code></pre><h4 id="원격-저장소-추가">원격 저장소 추가</h4><code>\$ git remote add [원격 저장소 이름][url]</code> 명령으로 아래는 예시이다.<pre><code>$ git remote add pb https://github.com/paulboone/ticgit
</code></pre><p>위와 같이 등록하고 실제로 로컬 저장소에는 없지만 pb 원격 저장소에 있는 것을 가져오려면 아래와 같이 명령한다.</p><pre><code>$ git fetch pb
</code></pre><p>git fetch 명령은 원격 저장소의 데이터를 모두 로컬로 가져오지만 자동으로 Merge하지는 않는다.</p><h4 id="원격-저장소에-push하기">원격 저장소에 PUSH하기</h4><code>git push [원격 저장소 이름][브랜치 이름] </code> 명령으로 Push할 수 있다.<pre><code>$ git push origin master
// origin 원격 저장소의 master 브랜치에 Push한다.
</code></pre><pre><code>$ git push origin HEAD
// 원격 저장소에 현재 로컬 브랜치와 동일한 이름의 브랜치에 Push한다.
// 원격 저장소의 다른 브랜치에 잘못 커밋하는 실수를 줄여줄 수 있다.
</code></pre><h4 id="원격-저장소-이름을-바꾸거나-삭제하기">원격 저장소 이름을 바꾸거나 삭제하기</h4><pre><code>$ git remote rename pb paul
// 원격 저장소의 이름을 변경할 수 있다.
</code></pre><pre><code>$ git remote rm paul
//원격 저장소를 삭제할 수 있다.
</code></pre><h2 id="원격-저장소-이름-규칙">원격 저장소 이름 규칙</h2><p>전형적인 원격 저장소의 이름이다.</p><code>origin</code>: Github 내 저장소의 이름(Fork한 경우 포함)<code>upstream</code>: Fork한 경우 원본 프로젝트의 저장소의 이름(디폴트 이름은 아니지만 통상적인 이름) 참고로 master는 브랜치 이름이다.<code>master</code>: git init으로 저장소 생성 시 디폴트 브랜치 이름<h2 id="브랜치">브랜치</h2><h4 id="브랜치-만들기">브랜치 만들기</h4><pre><code>$ git branch testing
// git branch [브랜치 이름] 명령으로 아래와 같이 testing 브랜치를 만든다.
</code></pre><h4 id="브랜치-변경">브랜치 변경</h4><pre><code>$ git checkout testing
// git checkout 명령어로 작업하려는 testing 브랜치로 이동할 수 있다.
</code></pre><p>브랜치를 이동(변경)하면 작업 디렉토리의 파일 내용도 변경된다.</p><pre><code>$ git checkout -b testing
// git checkout -b 명령어로 두 번 입력하지 않고
// 한 번에 브랜치를 만들고 바로 이동할 수도 있다.
</code></pre><h4 id="로컬-브랜치를-원격-저장소에-push하기">로컬 브랜치를 원격 저장소에 PUSH하기</h4><pre><code>$ git push -u origin testing
// git push -u origin [브랜치 이름] 명령으로
// 로컬 브랜치를 원격 저장소에도 반영할 수 있다.
</code></pre><hr/><h4 id="merge">MERGE</h4><h6 id="이슈-처리를-위해-브랜치-적용-시나리오">이슈 처리를 위해 브랜치 적용 시나리오</h6><pre><code>$ git checkout -b issue53
// 어떤 이슈 처리를 위한 issue53 토픽 브랜치를 만든다.
</code></pre><pre><code>$ vim index.html
// 소스파일을 수정한다.
</code></pre><pre><code>$ git commit -am &#x27;fixed the broken email address&#x27;
// 수정한 파일을 로컬 저장소로 커밋한다.
</code></pre><pre><code>$ git checkout master
// master 브랜치로 다시 이동한다.
</code></pre><pre><code>$ git merge issue53
// issue53 브랜치의 내용을 master 브랜치에 반영하여 합친다.
</code></pre><pre><code>$ git branch -d issue53
// 브랜치가 불필요해지면 삭제한다.
</code></pre><h4 id="remote-clone--branch-pull">REMOTE CLONE &amp; BRANCH PULL</h4><h6 id="리모트-clone-후-branch-도-clone-하는-방법">리모트 CLONE 후 BRANCH 도 CLONE 하는 방법</h6><pre><code>$ git clone http://github.com/username/project.git
// 원하는 repository를 clone 하고
</code></pre><pre><code>$ git branch -a
// 모든 branch 확인
</code></pre><pre><code>$ git checkout -b branch-name origin/branch-name
// 원하는 BRANCH로 -b 옵션으로 checkout 하면 로컬에서 해당 branch 가능
</code></pre><hr/><h2 id="기타-유용한-명령어">기타 유용한 명령어</h2><h4 id="1-git-stash">1. git stash</h4><p>커밋 후 깨끗한 상태에서 나름 작업했던 것을 커밋하지 않고 임시로 stash(안전한 곳에 넣어두다)할 수 있다.
나중에 stash한 상태의 코드를 다시 불러 올 수 있다.</p><pre><code>$ git stash save &quot;mystash&quot;
// 현재 작업을 저장해두고 branch를 head로 돌린다. (git reset -hard)
</code></pre><pre><code>$ git stash list
// 저장되어 있는 stash들 보기
</code></pre><pre><code>$ git stash pop
// stash들은 스택에 저장된다. 따라서 pop 명령은 가장 최근에 stash 한 상태를 현재 branch에 적용하고,
// stash가 저장되어 있는 stack 에서 삭제된다.
</code></pre><pre><code>$ git stash apply
// pop 과 비슷한 명령이지만 stash list에서 삭제하기 않고 그냥 적용만 시킨다.
</code></pre><pre><code>$ git stash drop
// 필요없는 stash를 삭제
</code></pre><pre><code>$ git stash clear
// 전체 stash list를 삭제
</code></pre></div></div>]]></content:encoded>
        </item>
        <item>
            <title><![CDATA[Github Pages 사용법]]></title>
            <link>\posts\2019-01-12\github-pages-tutorial\</link>
            <guid>\posts\2019-01-12\github-pages-tutorial\</guid>
            <content:encoded><![CDATA[<div><div><h2 id="github-pages-사용법">Github Pages 사용법</h2><h3 id="나만의-홈페이지-만들기">나만의 홈페이지 만들기</h3><p>NodeJS, ReactJS를 만지다 보면 내가 만든 SPA앱을 호스팅하고 싶은데요.
제가 쓰는 SPA앱용 호스팅은 <a href="http://netlify.com">netlify.com</a>를 주로 이용합니다.
무료이고 속도도 제일 빠르더라구요.</p><p>netlify는 heroku와 함께 프론트엔드, 백엔드 무료 서비스의 대주자라고 생각합니다.
잘 이용하시면 나만의 웹앱을 꽁짜로 서비스 할 수 있네요.</p><p>물론 상업적으로 론칭할려면 AWS 같은 유로 호스팅 서비스로 가야겠지만요.</p><h2 id="나만의-홈페이지인데-블로그를-만들고-싶다며">나만의 홈페이지인데 블로그를 만들고 싶다며</h2><p>블로그는 <a href="https://gatsby.com">Gatsby</a>가 기능이 많고 좋은데 사실 하나 만들어 봤습니다.
netlify에 호스팅했었는데 한번 방문 해 보세요.</p><p><a href="http://cpro95.netlify.com">cpro95.netlify.com</a></p><p>사실 Gatsby 블로그 시스템도 성능이나 속도면에서 좋지만 좀 무거운 면이 없지 않아서
다른 걸 찾다가 reddit을 보다가 우연히 새로 발견한게 있는데
James K Nelson의 &quot;npx create-react-blog&quot;입니다.
React용 라우터인 Navi를 만든 사람 같은데 이 블로그 템플릿도 만들었더라구요.
조금 만져 보니까 가볍고 쉽더라구요.</p><p>그래서 github pages와 함께 사용할 웹앱으로 결정했습니다.</p><p><a href="https://cpro95.github.io">cpro95.github.io</a></p><h2 id="github-pages-본격-사용법">Github Pages 본격 사용법</h2><h3 id="리포지터리-만들기">리포지터리 만들기</h3><p>일단 github.io에 정적 사이트를 호스팅 할려면 먼저 리포지터리를 만들어야 겠죠?
일단 github.com 에서 자신의 아이디로 된 리포지터리를 github pages용으로 만듭니다.
제 경우 아이디가 cpro95이기 때문에 cpro95.github.io 이란 좀 긴 리포지터리를 만들었습니다.
이제 터미널에서 클론 해보죠</p><pre><code class="language-bash" data-language="bash" data-highlighted-line-numbers=""><span class="token function">git</span> clone https://github.com/cpro95/cpro95.github.io.git
</code></pre><p>username.github.io 의 호스팅은 master 브랜치에 저장된 데이터만 적용됩니다.
로컬에서 데이터 수정후 remote 로  push 하면 github.io가 자동으로 호스팅 합니다.</p><p>여기서 헷갈리는게 있는데 github pages는 사용자 페이지와 프로젝트 페이지가 있습니다.
사용자 페이지는 username.github.io 와 같이 자신의 github.io 페이저 첫 부분입니다.
사용자 페이지는 master 브랜치를 통해서만 편집할 수 있구요.</p><p>프로젝트 페이지는 쉽게 설명하면 본인이 만들고 있는 웹앱이나 홈페이지가 있는데
이걸 자신의 블로그 밑에 정적으로 호스팅하고 싶을때 사용하는 페이지 입니다.</p><p>예를 들어 카카오톡 링크보내기 웹앱을 만들었고 github.io에 호스팅하고 싶다면
즉, kakao-cpro95.git 이라는 프로젝트를 github pages로 정적 호스팅하고 싶다면
<a href="https://cpro95.github.io/kakao-cpro95/">https://cpro95.github.io/kakao-cpro95/</a> 란 웹경로로 호스팅 됩니다.</p><p>프로젝트 페이지는 gh-pages란 브랜치를 만들고 package.json 의 homepage란을 적절히 작성하면
gh-page deploy로 바로 퍼블리싱할 수 있습니다.</p><p>gh-page란 브랜치가 자동으로 자신의 github.io 밑에 퍼블리싱되구요.</p><p>다음에 좀더 gh-page 사용법에 대해 알아보겠습니다.</p></div></div>]]></content:encoded>
        </item>
    </channel>
</rss>